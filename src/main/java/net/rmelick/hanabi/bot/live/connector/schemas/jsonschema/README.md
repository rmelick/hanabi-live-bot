These files should be generated by the quicktype cli
using `./gradlew generateJsonSchemaFromSamples`, but then 
modified / tweaked by hand so they reflect the spec of hanabi-live
as closely as possible