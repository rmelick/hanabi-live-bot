package net.rmelick.hanabi.backend;

/**
 * Takes requests over the public api and maps them to internal calls to the GameManager
 *  - extracts the user information from the session
 *  - validates that they are valid moves (or catches the errors that causes?)
 */
public class ClientToBackendAdapter {
}
